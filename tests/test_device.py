#!/usr/bin/env python

"""Test suite for the device class."""

# build-in modules

# third-party modules
# import pytest
# from graphene.test import Client

# changes to the path ...

# project modules
# from tangogql.schema import tangoschema
# import queries

# __author__ = "antmil, Linh Nguyen"
# __docformat__ = "restructuredtext"


# @pytest.fixture
# def client(scope='session'):
#     client = Client(tangoschema)
#     return client

# def execute(client, query):
#     return (client.execute(query))['data']


# def test_device_resolve_name(client):
#     result = execute(client, queries.device_name)
#     assert ('devices' in result) == True
#     assert ("name"  in result['devices'][0]) == True
#     assert "sys/tg_test/1" == result['devices'][0]['name']
